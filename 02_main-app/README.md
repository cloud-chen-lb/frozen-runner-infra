# 02_main-app

Owns Cloud Build, Artifact Registry, main-app NAT, PostgreSQL Cloud SQL, Secret Manager, service accounts, and Cloud Run deployment.

The local test scripts use mock `gcloud` commands and do not provision live GCP resources.
